{
    // Configuració d'un codespace demo pel curs de VSC del TAV
    // Fitxer amb les intruccions per crear el container i el directori de treball
    "build": {
        "dockerfile": "./Dockerfile",
        "context": "."
    },
    "name": "Codespace demo curs VSC - Python",
    // Configure tool-specific properties.
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-vscode-remote.remote-containers"
            ]
        }
    },
    "settings": {
        "git.autofetch": true
    },
}
